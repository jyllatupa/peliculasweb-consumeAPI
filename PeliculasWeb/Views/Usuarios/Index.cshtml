@{
    ViewData["Title"] = "Lista de Usuarios";
}

<div class="container py-4">
    <div class="row mb-4">
        <div class="col-8">
            <h2 class="text-primary">
                <i class="fas fa-list-alt"></i> Lista de Usuarios
            </h2>
        </div>
    </div>

    <div class="card shadow-sm rounded-4 bg-dark text-light">
        <div class="card-body p-4">
            <div class="table-responsive">
                <table id="tblUsuarios" class="table table-dark table-hover table-striped table-bordered align-middle mb-0 rounded-3 overflow-hidden">
                    <thead class="table-secondary text-dark">
                        <tr>
                            <th scope="col" class="d-none">Id</th>
                            <th scope="col">Nombre</th>
                            <th scope="col">Nombre Usuario</th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Aquí irán las filas generadas por DataTables -->
                    </tbody>
                </table>
            </div>
        </div>
    </div>

    <!--<div class="table-responsive">
        <table id="tblUsuarios" class="table table-striped table-bordered">
            <thead class="table-dark">
                <tr>
                    <th scope="col">Id</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Nombre Usuario</th>
                </tr>
            </thead>
            <tbody>-->
    <!-- Aquí irán las filas generadas por DataTables -->
    <!--</tbody>
        </table>
    </div>-->
</div>

@section Scripts {
    <script src="~/js/usuario.js"></script>
}